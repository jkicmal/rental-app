import React from 'react';
import { FlexContainer } from '../common';

const EmployeeRentalProduct = ({ product }) => {
  return <FlexContainer>Test</FlexContainer>;
};

export default EmployeeRentalProduct;
